/* 微信小程序兼容的动画效果 */
.bn-tabbar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  height: 120rpx;
  background: #ffffff;
  border-top: 1rpx solid #e5e7eb;
  display: flex;
  align-items: center;
  justify-content: space-around;
  z-index: 1000;
  padding-bottom: env(safe-area-inset-bottom);
  box-shadow: 0 -4rpx 20rpx rgba(0, 0, 0, 0.08);
  overflow: hidden;
}

.bn-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  position: relative;
  transition: background-color 0.3s ease;
  border-radius: 24rpx;
  padding: 16rpx 8rpx;
}

/* 鼠标悬停效果 */
.bn-item:hover { background: transparent; transform: none; }
.bn-item:hover .bn-icon { transform: none; }
.bn-item:hover .bn-text { opacity: 1; }

/* 点击效果 */
.bn-item:active { transform: none; background: transparent; }

/* 激活状态 */
.bn-item.active {
  background: rgba(16, 185, 129, 0.12);
}

/* 激活状态下的悬停效果 */
.bn-item.active:hover { background: rgba(16, 185, 129, 0.12); transform: none; }

.bn-icon {
  width: 48rpx;
  height: 48rpx;
  transition: transform 0.3s ease;
}

.bn-icon-wrap{ position: relative; }
.bn-dot{ position: absolute; right: -4rpx; top: -4rpx; width: 12rpx; height: 12rpx; border-radius: 50%; background: #ef4444; box-shadow: 0 0 0 2rpx #ffffff; }

.bn-item.active .bn-icon {
  transform: scale(1.15);
  filter: none;
  opacity: 1;
}

.bn-text {
  font-size: 22rpx;
  font-weight: 500;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Helvetica Neue', Arial, sans-serif;
  transition: all 0.3s ease;
  opacity: 0.7;
}

.bn-item.active .bn-text {
  opacity: 1;
  font-weight: 600;
}

/* 添加底部指示器 */
.bn-item::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  width: 0;
  height: 4rpx;
  background: #10B981;
  border-radius: 2rpx;
  transform: translateX(-50%);
  transition: width 0.3s ease;
}

.bn-item.active::after {
  width: 32rpx;
}

/* 悬停时的指示器效果 */
.bn-item:hover::after { width: 0; opacity: 1; }
