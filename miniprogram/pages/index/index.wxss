/* pages/index/index.wxss */

.page-container {
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
  min-height: 100vh;
  /* 为固定底部导航预留空间（含安全区） */
  padding-bottom: calc(160rpx + env(safe-area-inset-bottom));
}

/* 固定头部样式 */
.fixed-header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  background: linear-gradient(135deg, #4CAF50 0%, #26A69A 50%, #00BCD4 100%);
  padding: 24rpx 32rpx;
  padding-top: 88rpx;
  box-shadow: 0 4rpx 20rpx rgba(76, 175, 80, 0.3);
}

.header-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.header-left {
  display: flex;
  align-items: center;
}

.app-icon {
  width: 80rpx;
  height: 80rpx;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 24rpx;
  backdrop-filter: blur(10rpx);
}

.icon-heart {
  color: #ffffff;
  font-size: 36rpx;
}

.app-info {
  display: flex;
  flex-direction: column;
}

.app-title {
  color: #ffffff;
  font-size: 36rpx;
  font-weight: bold;
  line-height: 1.2;
}

.app-subtitle {
  color: rgba(255, 255, 255, 0.8);
  font-size: 24rpx;
  margin-top: 4rpx;
}

.header-right {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

/* 统一：与个人中心一致的头部通知按钮样式 */
.header-action-btn {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.25);
  backdrop-filter: blur(12rpx);
  display: flex;
  align-items: center;
  justify-content: center;
}
.header-action-btn:active { transform: scale(0.96); }
.header-action-icon { width: 36rpx; height: 36rpx; }
.header-action-badge {
  position: absolute;
  top: -6rpx;
  right: -6rpx;
  width: 18rpx;
  height: 18rpx;
  background: #ef4444;
  border-radius: 50%;
  border: 4rpx solid rgba(255,255,255,0.85);
  box-shadow: 0 4rpx 8rpx rgba(239, 68, 68, 0.35);
}

.icon-notification,
.icon-more {
  color: #ffffff;
  font-size: 32rpx;
}

/* Image-based icons sizing */
.icon-notification-img { width: 32rpx; height: 32rpx; }
.stat-icon-img { width: 40rpx; height: 40rpx; display: block; }
.stat-icon-mask {
  width: 40rpx;
  height: 40rpx;
  display: block;
  background-color: #ffffff; /* 默认白色，确保在彩色背景上清晰可见 */
  -webkit-mask-repeat: no-repeat;
  mask-repeat: no-repeat;
  -webkit-mask-position: center;
  mask-position: center;
  -webkit-mask-size: contain;
  mask-size: contain;
}
.inline-title-icon { width: 28rpx; height: 28rpx; margin-right: 12rpx; vertical-align: -4rpx; }
.action-icon-img { width: 44rpx; height: 44rpx; display: block; }
.action-icon-mask {
  width: 44rpx;
  height: 44rpx;
  display: block;
  background-color: #ffffff; /* 默认填充色，可被内联覆盖 */
  -webkit-mask-repeat: no-repeat;
  mask-repeat: no-repeat;
  -webkit-mask-position: center;
  mask-position: center;
  -webkit-mask-size: contain;
  mask-size: contain;
}
.activity-icon-img { 
  width: 40rpx; 
  height: 40rpx; 
  display: block;
}

/* 背景图方式渲染活动图标，避免 <image> 对 SVG 的裁剪问题 */
.activity-icon-bg {
  width: 48rpx;
  height: 48rpx;
  display: block;
  /* 使用 mask 渲染矢量图标，避免 iOS 对 background-image 的裁剪/解析问题 */
  background-color: #ffffff; /* 图标填充色 */
  -webkit-mask-repeat: no-repeat;
  mask-repeat: no-repeat;
  -webkit-mask-position: center;
  mask-position: center;
  -webkit-mask-size: contain;
  mask-size: contain;
  transform: translateY(-1rpx); /* 修复部分设备底部像素裁剪 */
}
.alert-icon-img { width: 40rpx; height: 40rpx; }
.modal-close-icon-img { width: 32rpx; height: 32rpx; }
.label-icon-img { width: 28rpx; height: 28rpx; margin-right: 12rpx; vertical-align: -4rpx; }
.upload-icon-img { width: 48rpx; height: 48rpx; }
.photo-action-icon-img { width: 28rpx; height: 28rpx; }
.login-icon-img { width: 120rpx; height: 120rpx; }

/* 主要内容区域 */
.main-content {
  padding-top: 32rpx;
  padding-left: 32rpx; /* 恢复全局左右边距为24rpx */
  padding-right: 32rpx; /* 避免影响其它卡片 */
}

/* 欢迎卡片样式 */
.welcome-card {
  background: linear-gradient(135deg, #4CAF50 0%, #26A69A 50%, #00BCD4 100%);
  color: #ffffff;
  padding: 48rpx;
  border-radius: 48rpx;
  margin-bottom: 32rpx;
  box-shadow: 0 16rpx 48rpx rgba(76, 175, 80, 0.3);
  position: relative;
  overflow: hidden;
  min-height: 220rpx; /* 与鹦鹉页顶部卡片统一高度 */
}

.welcome-bg-circle {
  position: absolute;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 50%;
}

.circle-1 {
  width: 256rpx;
  height: 256rpx;
  top: -128rpx;
  right: -128rpx;
}

.circle-2 {
  width: 192rpx;
  height: 192rpx;
  bottom: -96rpx;
  left: -96rpx;
}

.welcome-content {
  position: relative;
  z-index: 10;
}

.welcome-greeting {
  font-size: 40rpx;
  font-weight: bold;
  display: block;
  margin-bottom: 16rpx;
}

.welcome-message {
  font-size: 32rpx;
  color: rgba(255, 255, 255, 0.9);
  display: block;
  margin-bottom: 32rpx;
  white-space: nowrap;
}
.welcome-day-count {
  font-size: 44rpx;
  font-weight: 800;
  color: #FFFFFF;
}

.status-indicator {
  display: flex;
  align-items: center;
}

.status-dot {
  width: 16rpx;
  height: 16rpx;
  background: #ffffff;
  border-radius: 50%;
  margin-right: 16rpx;
  animation: pulse 2s infinite;
  flex-shrink: 0;
}

.status-dot-healthy {
  background: #4CAF50;
  box-shadow: 0 0 8rpx rgba(76, 175, 80, 0.5);
}

.status-dot-warning {
  background: #FF9800;
  box-shadow: 0 0 8rpx rgba(255, 152, 0, 0.5);
  animation: pulse-warning 1.5s infinite;
}

.status-dot-recovering {
  background: #2196F3;
  box-shadow: 0 0 8rpx rgba(33, 150, 243, 0.5);
  animation: pulse-recovering 2s infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

@keyframes pulse-warning {
  0%, 100% { 
    opacity: 1; 
    transform: scale(1);
  }
  50% { 
    opacity: 0.7; 
    transform: scale(1.1);
  }
}

@keyframes pulse-recovering {
  0%, 100% { 
    opacity: 1; 
    transform: scale(1);
  }
  33% { 
    opacity: 0.8; 
    transform: scale(1.05);
  }
  66% { 
    opacity: 0.6; 
    transform: scale(1.1);
  }
}

.status-text {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.8);
  line-height: 30rpx;
}

.stat-card {
  background: #ffffff; /* 不透明白底 */
  padding: 48rpx; /* 保持体量感 */
  border-radius: 32rpx;
  box-shadow: 0 12rpx 30rpx rgba(0, 0, 0, 0.08); /* 轻柔阴影 */
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  text-align: left; /* 卡片内文字左对齐 */
}

.stat-card:active {
  transform: translateY(4rpx) scale(0.98);
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.12); /* 活跃态阴影稍加强 */
}

.stat-icon-container {
  width: 64rpx; /* 小徽章尺寸 */
  height: 64rpx;
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 20rpx;
}

.stat-icon-container.emerald {
  background: rgba(76, 175, 80, 0.1);
}

.stat-icon-container.orange {
  background: rgba(255, 152, 0, 0.1);
}

.stat-icon-container.green {
  background: rgba(76, 175, 80, 0.1);
}

.stat-icon-container.blue {
  background: rgba(33, 150, 243, 0.1);
}

.stat-icon-container.purple {
  background: rgba(156, 39, 176, 0.1);
}

.stat-icon {
  font-size: 40rpx;
}

.stat-value {
  font-size: 52rpx;
  font-weight: bold;
  margin-bottom: 8rpx;
  display: block;
  text-align: left; /* 数值左对齐 */
}

.emerald-text {
  color: #4CAF50;
}

.orange-text {
  color: #FF9800;
}

.green-text {
  color: #4CAF50;
}

.blue-text {
  color: #2196F3;
}

.purple-text {
  color: #9C27B0;
}

/* 弹窗覆盖层 */
.modal-overlay {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  backdrop-filter: blur(8rpx);
  z-index: 2000;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 32rpx;
}

/* 弹窗容器 */
.modal-content {
  width: 100%;
  max-width: 700rpx;
  max-height: 82vh;
  background: #ffffff;
  border-radius: 48rpx;
  overflow: hidden;
  box-shadow: 0 20rpx 60rpx rgba(0,0,0,0.18);
  font-family: 'Microsoft YaHei','PingFang SC','Hiragino Sans GB','Heiti SC','SimHei',system-ui,-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,'Noto Sans',sans-serif;
}

/* 渐变头部 */
.modal-header-gradient {
  background: linear-gradient(135deg, #4CAF50 0%, #26A69A 50%, #00BCD4 100%);
  padding: 24rpx 32rpx;
  position: relative; /* 使关闭按钮可绝对定位到右上角 */
}

.modal-header-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.modal-title {
  color: #ffffff;
  font-size: 36rpx;
  font-weight: bold;
}

.modal-close-btn {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.2);
  display: flex;
  align-items: center;
  justify-content: center;
  position: absolute;
  top: 16rpx;
  right: 16rpx;
}

.modal-close-icon {
  color: #ffffff;
  font-size: 32rpx;
}

/* 滚动内容区 */
.modal-scroll {
  max-height: calc(82vh - 240rpx);
}

.form-section {
  padding: 32rpx;
}

.form-item { margin-bottom: 24rpx; }

.form-label {
  display: block;
  font-size: 26rpx;
  color: #374151;
  font-weight: 600;
  margin-bottom: 12rpx;
}

/* 统一弹窗表单字段字体为中文黑体（无衬线） */
.modal-content .form-label,
.modal-content .text-input,
.modal-content .picker-display,
.modal-content .textarea-input {
  font-family: 'Microsoft YaHei','PingFang SC','Hiragino Sans GB','Heiti SC','SimHei',system-ui,-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,'Noto Sans',sans-serif;
}

.label-icon { color: #10B981; margin-right: 12rpx; }

.text-input {
  width: 100%;
  box-sizing: border-box;
  padding: 0 24rpx; /* 仅水平内边距，配合固定高度 */
  border: 2rpx solid #e5e7eb;
  border-radius: 32rpx;
  font-size: 26rpx;
  height: 88rpx; /* 提高输入框高度，避免文字被压缩 */
}
/* 输入框聚焦态优化 */
.text-input:focus {
  border-color: #26A69A;
  background: #ffffff;
}

.textarea-input {
  width: 100%;
  box-sizing: border-box;
  padding: 24rpx;
  border: 2rpx solid #e5e7eb;
  border-radius: 32rpx;
  font-size: 26rpx;
  min-height: 160rpx; /* 文本域更高，便于输入与显示 */
}

.form-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24rpx;
}

.picker-wrapper { position: relative; }

.picker-display {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx;
  border: 2rpx solid #e5e7eb;
  border-radius: 32rpx;
  font-size: 26rpx;
  background: #ffffff;
}
/* 选择器按压反馈 */
.picker-display:active {
  border-color: #26A69A;
}

.picker-arrow { color: #9CA3AF; }

.gender-row {
  display: flex;
  gap: 16rpx;
}

.gender-btn {
  flex: 1;
  text-align: center;
  padding: 20rpx;
  border-radius: 24rpx;
  font-size: 26rpx;
  border: 2rpx solid transparent;
}

.gender-male { background: #f1f5ff; color: #1e40af; }
.gender-male.active { background: #dbeafe; border-color: #93c5fd; color: #1e3a8a; }
.gender-female { background: #fff1f5; color: #9d174d; }
.gender-female.active { background: #ffe4e6; border-color: #f9a8d4; color: #831843; }

.modal-footer {
  display: flex;
  gap: 12rpx; /* 按钮间距更紧凑 */
  padding: 20rpx 28rpx; /* 底部整体内边距略减 */
  border-top: 2rpx solid #f3f4f6;
  background: #f9fafb;
}

.btn-secondary,
.btn-primary {
  flex: 1;
  padding: 18rpx 24rpx; /* 按钮高度更薄 */
  border-radius: 28rpx; /* 圆角略收窄 */
  font-size: 26rpx; /* 字体稍微减小 */
}

.btn-secondary { background: #e5e7eb; color: #374151; }
.btn-primary { background: linear-gradient(135deg,#4CAF50,#26A69A,#00BCD4); color: #ffffff; }
.btn-primary.is-disabled { opacity: 0.5; }

.stat-label {
  font-size: 28rpx;
  color: #666666;
  font-weight: 400; /* 常规字重 */
  text-align: left; /* 标签左对齐 */
}

/* 快速操作区域样式 */
.quick-actions-card {
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(20rpx);
  border-radius: 48rpx;
  padding: 24px;
  margin-bottom: 48rpx;
  box-shadow: 0 12rpx 32rpx rgba(0, 0, 0, 0.08); /* 中性阴影 */
  border: 1rpx solid rgba(255, 255, 255, 0.5);
}

.card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 40rpx;
}

.card-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #333333;
}

.actions-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 24rpx; /* 对齐APP，更紧凑的按钮间距 */
}

.quick-actions-card {
  overflow: hidden;
  box-sizing: border-box;
}

.action-item {
  min-width: 0; /* 允许网格子项收缩 */
}

.action-label {
  max-width: 100%;
  display: -webkit-box;
  -webkit-line-clamp: 2; /* 最多两行 */
  -webkit-box-orient: vertical;
  white-space: normal;
  overflow: hidden; 
  word-break: break-word;
  padding:0rpx 12rpx;
}

/* 统一按钮风格与APP设计 */
.action-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 32rpx 24rpx; /* 与APP一致的内边距 */
  border-radius: 32rpx;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  background: linear-gradient(135deg, #f8fafc, #f1f5f9);
  border: 2rpx solid transparent; /* 默认透明，主题色单独设置 */
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.06);
}

/* 去除按钮边框，统一为无边框 */
.action-item {
  border: none;
}

/* 快捷操作背景按APP柔和主题色（不透明） */
.action-item.feeding {
  background: linear-gradient(135deg, #FFF4DF, #FEE8BF);
}
.action-item.health {
  background: linear-gradient(135deg, #faf5ff, #f3e8ff);
}
.action-item.training {
  background: linear-gradient(135deg, #E6F6FF, #D6EEFF);
}
.action-item.pairing {
  background: linear-gradient(135deg, #F3E8FF, #E9D5FF);
}
.action-item.add,
.action-item.add-expense {
  background: linear-gradient(135deg, #E9FFF4, #D7F8E7);
}
.action-item.expenses {
  background: linear-gradient(135deg, #EAF7FF, #D9EEFF);
}
.action-item.management {
  background: linear-gradient(135deg, #FFF2E6, #FDE2CC);
}
.action-item.Careguide {
  background: linear-gradient(135deg, #F3E8FF, #E9D5FF);
}
.action-item.cleaning,
.action-item.guide {
  background: linear-gradient(135deg, #dbeafe, #bfdbfe);
}

/* 活跃态不改背景，仅缩放与阴影（遵循APP） */
.action-item:active {
  transform: translateY(4rpx) scale(0.95);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.14);
}

/* 按APP设计：快捷操作按钮无边框 */

/* 已统一至上方.active定义，避免重复与覆盖 */

/* 图标容器保持APP风格的卡感 */
.action-icon-wrapper {
  width: 112rpx;
  height: 112rpx;
  border-radius: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.1);
  background: #ffffff;
  transition: transform 0.3s ease;
  line-height: 0; /* 避免内联基线影响导致图标被裁剪 */
}

.action-item:active .action-icon-wrapper {
  transform: scale(1.05);
}

.action-icon {
  font-size: 40rpx; /* 与APP一致 */
}

/* 文案风格与APP一致（去除加粗） */
.action-label {
  font-size: 26rpx; /* 与APP统一 */
  color: #374151; /* 深灰提高可读性 */
  text-align: center;
  font-weight: 400; /* 常规字重，不加粗 */
  line-height: 1.2;
}

/* APP规范：按钮无边框，不设置主题边框色 */

/* 禁用态保持统一 */
.action-item.disabled {
  opacity: 0.5;
  pointer-events: none;
}

.action-item.disabled .action-icon-wrapper { filter: grayscale(100%); }
.action-item.disabled .action-label { color: #999999; }

/* 最近活动区域样式 */
.recent-activity-card {
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(20rpx);
  border-radius: 48rpx;
  padding: 24rpx;
  margin-bottom: 32rpx;
  box-shadow: 0 12rpx 32rpx rgba(0, 0, 0, 0.08); /* 中性阴影 */
  border: 1rpx solid rgba(255, 255, 255, 0.5);
}

/* 健康提醒卡片：与最近活动卡片对齐 */
.health-alerts .card {
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(20rpx);
  border-radius: 48rpx;
  padding: 24rpx; /* 与其他卡片相同的内边距 */
  margin: 0 0 32rpx 0; /* 覆盖全局 .card 的左右 margin，保持对齐 */
  box-shadow: 0 12rpx 32rpx rgba(0, 0, 0, 0.08);
  border: 1rpx solid rgba(255, 255, 255, 0.5);
}

/* 外层容器填满并无额外左右间距，保证与其它卡片对齐 */
.health-alerts {
  width: 100%;
  margin-left: 0;
  margin-right: 0;
}

.health-alerts .card-content {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.alert-list {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.alert-item {
  display: flex;
  align-items: center;
  padding: 32rpx;
  border-radius: 32rpx;
  position: relative;
  transition: all 0.3s ease;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.08);
  border: 1rpx solid rgba(255, 255, 255, 0.5);
  overflow: visible;
}

.alert-item:active {
  transform: scale(0.98);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.12);
}

/* 严重程度主题背景与左侧强调条 */
.alert-item::before {
  content: '';
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 8rpx;
  border-top-left-radius: 32rpx;
  border-bottom-left-radius: 32rpx;
}

.alert-item.high {
  background: linear-gradient(135deg, rgba(239, 68, 68, 0.12), rgba(255, 153, 153, 0.12));
}
.alert-item.high::before { background: #ef4444; }

.alert-item.medium {
  background: linear-gradient(135deg, rgba(245, 158, 11, 0.12), rgba(255, 193, 7, 0.12));
}
.alert-item.medium::before { background: #f59e0b; }

.alert-item.low {
  background: linear-gradient(135deg, rgba(20, 184, 166, 0.12), rgba(45, 212, 191, 0.12));
}
.alert-item.low::before { background: #14b8a6; }

.alert-icon {
  width: 96rpx; /* 与最近活动图标容器统一尺度 */
  height: 96rpx;
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.1);
  background: #ffffff;
}

.alert-icon-img {
  width: 64rpx;
  height: 64rpx;
}

.alert-content {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.alert-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #333333;
}

.alert-subtitle {
  font-size: 26rpx;
  color: #666666;
}

.more-link {
  font-size: 28rpx;
  color: #10b981;
  font-weight: 500;
}

.activity-list {
  display: flex;
  flex-direction: column;
  gap: 32rpx;
}

.activity-item {
  display: flex;
  align-items: center;
  padding: 32rpx;
  border-radius: 32rpx;
  position: relative;
  transition: all 0.3s ease;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.08);
  border: 1rpx solid rgba(255, 255, 255, 0.5);
  overflow: visible; /* 防止内部元素被裁剪 */
}

.activity-item.orange-theme {
  background: linear-gradient(135deg, rgba(255, 152, 0, 0.1) 0%, rgba(255, 183, 77, 0.1) 100%);
}

.activity-item.green-theme {
  background: linear-gradient(135deg, rgba(76, 175, 80, 0.1) 0%, rgba(102, 187, 106, 0.1) 100%);
}

.activity-item.blue-theme {
  background: linear-gradient(135deg, rgba(33, 150, 243, 0.1) 0%, rgba(100, 181, 246, 0.1) 100%);
}

.activity-item.purple-theme {
  background: linear-gradient(135deg, rgba(156, 39, 176, 0.1) 0%, rgba(186, 104, 200, 0.1) 100%);
}

.activity-item:active {
  transform: scale(0.98);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.12);
}

.activity-icon-container {
  width: 80rpx;
  height: 80rpx;
  border-radius: 24rpx; /* 背景圆角与APP一致 */
  display: flex; /* 居中图标 */
  align-items: center;
  justify-content: center;
  margin-right: 24rpx; /* 与内容区间距 */
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.1); /* 轻阴影提升层次 */
  background: #ffffff; /* 兜底背景，主题类会覆盖 */
  overflow: visible; /* 防止图标被裁剪 */
  line-height: 0; /* 避免内联基线影响高度计算 */
}

.orange-theme .activity-icon-container {
  background: linear-gradient(135deg, #FF9800 0%, #FFB74D 100%);
}

.green-theme .activity-icon-container {
  background: linear-gradient(135deg, #22c55e 0%, #10b981 100%);
}

.purple-theme .activity-icon-container {
  background: linear-gradient(135deg, #9C27B0 0%, #BA68C8 100%);
}

.blue-theme .activity-icon-container {
  background: linear-gradient(135deg, #2196F3 0%, #64B5F6 100%);
}

.activity-icon {
  font-size: 40rpx; /* 增大图标尺寸 */
  color: #ffffff;
}

.activity-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.activity-title {
  font-size: 32rpx;
  color: #333333;
  font-weight: 600;
  line-height: 1.3;
  display: block;
}

.activity-subtitle {
  font-size: 28rpx;
  color: #666666;
  opacity: 0.8;
  line-height: 1.2;
  display: block;
}

.activity-dot {
  position: absolute;
  top: 32rpx;
  right: 32rpx;
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
}

.activity-dot.orange {
  background: #FF9800;
}

.activity-dot.green {
  background: #10b981;
}

.activity-dot.purple {
  background: #9C27B0;
}

.activity-dot.blue {
  background: #2196F3;
}

/* 登录提示卡片样式 */
.login-prompt-card {
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(20rpx);
  border-radius: 48rpx;
  padding: 48rpx;
  margin-bottom: 48rpx;
  box-shadow: 0 12rpx 32rpx rgba(0, 0, 0, 0.08); /* 中性阴影 */
  border: 1rpx solid rgba(255, 255, 255, 0.5);
}

.login-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

.login-icon {
  font-size: 120rpx;
}

.login-title {
  font-size: 40rpx;
  color: #333333;
  font-weight: bold;
  margin-bottom: 16rpx;
  display: block;
}

.login-subtitle {
  font-size: 28rpx;
  color: #666666;
  margin-bottom: 48rpx;
  display: block;
}

.login-btn {
  background: linear-gradient(135deg, #10b981 0%, #26A69A 50%, #00BCD4 100%);
  color: #ffffff;
  border: none;
  border-radius: 48rpx;
  padding: 6rpx 64rpx;
  font-size: 32rpx;
  font-weight: bold;
  box-shadow: 0 8rpx 24rpx rgba(76, 175, 80, 0.3);
  transition: all 0.3s ease;
}

.login-btn:active {
  transform: translateY(2rpx) scale(0.98);
  box-shadow: 0 4rpx 16rpx rgba(76, 175, 80, 0.4);
}

/* 禁用状态样式 */
.action-item.disabled {
  opacity: 0.5;
  pointer-events: none;
}

.action-item.disabled .action-icon-wrapper {
  filter: grayscale(100%);
}

.action-item.disabled .action-label {
  color: #999999;
}

/* 我的鹦鹉卡片样式 */
.my-parrots-card {
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(20rpx);
  border-radius: 48rpx;
  padding: 24rpx;
  margin-bottom: 32rpx;
  box-shadow: 0 12rpx 32rpx rgba(0, 0, 0, 0.08); /* 中性阴影 */
  border: 1rpx solid rgba(255, 255, 255, 0.5);
}

.parrot-card-scroll {
  width: 100%;
  overflow: hidden;
}
.parrot-card-row {
  display: flex;
  flex-direction: row;
  gap: 20rpx;
  padding: 4rpx 4rpx 8rpx 4rpx;
}

.my-parrots-card {
  overflow: hidden;
  box-sizing: border-box;
}

.parrot-mini-card {
  flex: 0 0 auto;
  min-width: 200rpx;
  width: 200rpx;
  box-sizing: border-box;
  background: #ffffff;
  border-radius: 32rpx;
  padding: 28rpx 24rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.08);
  border: none; /* 与APP一致：卡片无边框，仅阴影 */
}

.parrot-card-row.parrot-count-1 .parrot-mini-card,
.parrot-card-row.parrot-count-2 .parrot-mini-card {
  min-width: 260rpx;
  width: 260rpx;
}

.parrot-mini-avatar {
  width: 160rpx;
  height: 160rpx;
  border-radius: 24rpx;
  margin-bottom: 16rpx;
  background: #f7f7f7;
}

.parrot-mini-name,
.parrot-mini-species {
  max-width: 100%;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.parrot-mini-name {
  font-size: 30rpx;
  color: #333333;
  font-weight: 600;
  line-height: 1.2;
}

/* 名称与性别徽章容器 */
.parrot-mini-title {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

/* 迷你性别徽章 */
.mini-gender-badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  vertical-align: middle;
  margin-left: 4rpx;
  border: 2rpx solid transparent;
}
.mini-gender-icon {
  font-size: 16rpx;
  line-height: 1;
  font-weight: 700;
  transform: translateY(-1rpx); /* 最终居中小修 */
}
.mini-gender-icon-img {
  width: 16rpx;
  height: 16rpx;
}
.gender-male { background: #e0f2fe; border-color: #bae6fd; color: #0284c7; }
.gender-female { background: #fde7f3; border-color: #fbcfe8; color: #db2777; }
.gender-unknown { background: #f3f4f6; border-color: #e5e7eb; color: #6b7280; }

.parrot-mini-species {
  font-size: 26rpx;
  color: #888888;
  margin-top: 8rpx;
}

.parrot-mini-meta-row {
  margin-top: 8rpx;
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.parrot-mini-age {
  font-size: 24rpx;
  color: #666666;
}

.parrot-mini-badge {
  margin-top: 12rpx;
  padding: 6rpx 16rpx; /* 缩小徽章尺寸 */
  border-radius: 999rpx;
  font-weight: 600;
  font-size: 24rpx; /* 更小的文字尺寸 */
  color: #ffffff;
}

/* APP对齐：健康为蓝色，优秀为绿色 */
.parrot-mini-badge.healthy {
  background: rgba(37, 99, 235, 0.15); /* 浅蓝底 */
  color: #2563eb; /* 蓝色文字 */
}

.parrot-mini-badge.excellent {
  background: rgba(16, 185, 129, 0.15); /* 浅绿底 */
  color: #10b981; /* 绿色文字 */
}

.parrot-mini-badge.sick {
  background: rgba(239, 68, 68, 0.15); /* 浅红底 */
  color: #ef4444; /* 红色文字 */
}

.parrot-mini-badge.recovering {
  background: rgba(245, 158, 11, 0.15); /* 浅橙底 */
  color: #f59e0b; /* 橙色文字 */
}

.parrot-mini-badge.observation {
  background: rgba(37, 99, 235, 0.15); /* 浅蓝底 */
  color: #2563eb; /* 蓝色文字 */
}

/* 统一与“我的鹦鹉页面”列表使用的健康徽章样式 */
.health-badge {
  font-size: 18rpx;
  font-weight: 600;
  padding: 8rpx 16rpx;
  border-radius: 24rpx;
  color: #ffffff;
}
.health-badge.health-healthy {
  background: linear-gradient(135deg, #22c55e 0%, #10b981 100%);
}
.health-badge.health-sick {
  background: linear-gradient(135deg, #f44336 0%, #EF5350 100%);
}
.health-badge.health-recovering {
  background: linear-gradient(135deg, #FF9800 0%, #FFB74D 100%);
}
.health-badge.health-observation {
  background: linear-gradient(135deg, #2196F3 0%, #42A5F5 100%);
}









/* 首页添加鹦鹉弹窗：照片上传区样式 */
.photo-upload { padding: 16rpx 0; }
.photo-preview {
  position: relative;
  width: 220rpx;
  height: 220rpx;
  border-radius: 24rpx;
  overflow: hidden;
}
.preview-image { width: 100%; height: 100%; }
.photo-actions {
  position: absolute;
  top: 12rpx;
  right: 12rpx;
  display: flex;
  gap: 12rpx;
}
.action-btn {
  width: 60rpx;
  height: 60rpx;
  background: rgba(0,0,0,0.55);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}
.action-icon { color: #fff; font-size: 28rpx; }
.upload-area {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 220rpx;
  height: 220rpx;
  border: 2rpx dashed #d1d5db;
  border-radius: 24rpx;
  background: #f9fafb;
}
.upload-icon { font-size: 48rpx; }
.upload-text { margin-top: 12rpx; color: #6b7280; font-size: 24rpx; }

/* 编辑模式样式 */
.stats-grid.edit-mode {
  background: rgba(0, 0, 0, 0.05);
  border-radius: 24rpx;
  padding: 24rpx;
  margin: 0 20rpx 32rpx 20rpx;
}

.edit-toolbar {
  display: flex;
  justify-content: center;
  align-items: center;
  margin-bottom: 24rpx;
  padding: 0 8rpx;
}

.edit-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #1f2937;
}

/* 固定在右下角的完成按钮 */
.edit-done-btn-container {
  position: fixed;
  bottom: 200rpx; /* 在底部导航上方 */
  right: 32rpx;
  z-index: 1000;
}

.edit-done-btn-fixed {
  background: #10b981;
  color: white;
  border: none;
  border-radius: 50rpx;
  padding: 16rpx 32rpx;
  font-size: 28rpx;
  font-weight: 500;
  box-shadow: 0 8rpx 24rpx rgba(16, 185, 129, 0.3);
}

.edit-done-btn-fixed:active {
  background: #059669;
  transform: scale(0.95);
}

.widget-container {
  display: flex;
  flex-wrap: wrap;
  margin: 0 -8rpx 32rpx -8rpx; /* 通过左右内边距制造列间距 */
  width: 100%;
  box-sizing: border-box;
}

.widget-wrapper {
  position: relative;
  width: 50%;
  padding: 0 8rpx; /* 16rpx 的列间距 */
  margin: 12rpx 0 12rpx 0; /* 增大上下间距：上16rpx，下32rpx */
  box-sizing: border-box;
}

/* 体重趋势卡片占满整行 */
.widget-wrapper.full-row {
  width: 100%;
}

.widget-container.wiggle .widget-wrapper {
  animation: wiggle 1s ease-in-out infinite;
}

@keyframes wiggle {
  0%, 100% { transform: rotate(0deg); }
  25% { transform: rotate(-1deg); }
  75% { transform: rotate(1deg); }
}

.remove-btn {
  position: absolute;
  top: -8rpx;
  right: -8rpx;
  width: 48rpx;
  height: 48rpx;
  background: #ef4444;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10;
  box-shadow: 0 4rpx 12rpx rgba(239, 68, 68, 0.3);
}

.remove-btn:active {
  transform: scale(0.9);
  background: #dc2626;
}

.remove-icon {
  width: 24rpx;
  height: 24rpx;
}

.add-widgets-section {
  margin-top: 24rpx;
  padding-top: 24rpx;
  border-top: 2rpx solid rgba(0, 0, 0, 0.1);
}

.add-widgets-title {
  font-size: 28rpx;
  font-weight: 500;
  color: #6b7280;
  margin-bottom: 16rpx;
  text-align: center;
}

.add-widgets-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  justify-content: center;
}

.add-widget-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 20rpx;
  background: white;
  border-radius: 20rpx;
  border: 2rpx dashed #d1d5db;
  min-width: 120rpx;
  transition: all 0.3s ease;
}

.add-widget-item:active {
  transform: scale(0.95);
  border-color: #10b981;
  background: #f0fdf4;
}

.add-widget-icon {
  position: relative;
  margin-bottom: 8rpx;
}

.widget-icon {
  width: 48rpx;
  height: 48rpx;
}

.add-plus {
  position: absolute;
  top: -8rpx;
  right: -8rpx;
  width: 24rpx;
  height: 24rpx;
  background: #10b981;
  color: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20rpx;
  font-weight: bold;
}

.add-widget-name {
  font-size: 24rpx;
  color: #374151;
  text-align: center;
}

/* 公告横幅样式 */
.announcement-banner {
  margin: 16rpx;
  padding: 20rpx;
  background: linear-gradient(90deg, #4f46e5 0%, #6366f1 100%);
  color: #fff;
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  box-shadow: 0 8rpx 20rpx rgba(99, 102, 241, 0.25);
}
.announcement-content {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
  width: 86%;
}
.announcement-title {
  font-size: 28rpx;
  font-weight: 600;
}
.announcement-desc {
  font-size: 24rpx;
  opacity: 0.9;
}
.announcement-close {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: rgba(255,255,255,0.2);
}
.announcement-close-img {
  width: 28rpx;
  height: 28rpx;
}

.announcement-close-text {
  font-size: 32rpx;
  font-weight: 600;
  color: #fff;
  line-height: 48rpx;
}

/* 公告弹窗样式 */
.announcement-modal-overlay {
  position: fixed;
  z-index: 900;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(17, 24, 39, 0.5);
  backdrop-filter: blur(2rpx);
}
.announcement-modal {
  position: fixed;
  z-index: 901;
  left: 50%;
  top: 18%;
  transform: translateX(-50%);
  width: 86%;
  max-height: 60vh;
  background: rgba(255,255,255,0.92);
  border-radius: 24rpx;
  box-shadow: 0 24rpx 60rpx rgba(0, 0, 0, 0.2);
  overflow: hidden;
  display: flex;
  flex-direction: column;
}
.announcement-modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 24rpx;
  background: linear-gradient(135deg, #4CAF50 0%, #26A69A 50%, #00BCD4 100%);
  color: #fff;
}
.announcement-modal-title { font-size: 28rpx; font-weight: 700; }
.announcement-modal-close { width: 48rpx; height: 48rpx; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(255,255,255,0.2); }
.announcement-modal-close-text { font-size: 32rpx; font-weight: 600; color: #fff; line-height: 48rpx; }
.announcement-modal-body { padding: 24rpx; box-sizing: border-box; flex: 1; overflow-y: auto; -webkit-overflow-scrolling: touch; }
.announcement-modal-h1 { font-size: 28rpx; font-weight: 600; color: #111827; margin-bottom: 10rpx; }
.announcement-modal-content { font-size: 26rpx; color: #374151; line-height: 1.6; }
.announcement-modal-content { white-space: pre-wrap; word-break: break-word; }

.announcement-modal-images { display: block; margin: 12rpx 0; }
.announcement-image-inline { max-width: 100%; width: auto; height: auto; display: block; border-radius: 12rpx; background: #f3f4f6; margin: 8rpx 0; }

/* 统计卡片空状态样式 */
.empty-widgets-placeholder {
  margin-top: 24rpx;
  background: #ffffff;
  border-radius: 16rpx;
  box-shadow: 0 6rpx 20rpx rgba(0,0,0,0.06);
  padding: 32rpx;
  text-align: center;
}
.empty-title {
  font-size: 30rpx;
  color: #111827;
  font-weight: 600;
}
.empty-subtitle {
  margin-top: 12rpx;
  font-size: 24rpx;
  color: #6B7280;
}
.empty-reset-btn {
  margin-top: 20rpx;
  background-color: #4F46E5;
  color: #ffffff;
  border: none;
  border-radius: 999rpx;
  padding: 16rpx 28rpx;
  font-size: 26rpx;
}

/* 编辑模式遮罩与层级 */
.stats-grid {
  position: relative;
  display: flex;
  justify-content: center;
}
.edit-mode-mask {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(17, 24, 39, 0.35);
  border-radius: 20rpx;
  z-index: 0;
  pointer-events: none;
}
.widget-container {
  position: relative;
  z-index: 1;
}

/* 拖拽浮层样式 */
.drag-ghost {
  position: fixed;
  z-index: 9;
  opacity: 0.95;
  transform: scale(1.02);
  pointer-events: none;
  box-shadow: 0 12rpx 28rpx rgba(0,0,0,0.15);
}
/* 删除左滑样式，改为长按弹出ActionSheet，因此不再需要 */

/* ----------------------------------
   首页自定义引导动画样式
   ---------------------------------- */

.gesture-guide-container {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 100;
  pointer-events: auto;
  background: rgba(0, 0, 0, 0.02); /* 极淡的背景遮罩 */
  overflow: visible;
}

.gesture-target-highlight {
  position: absolute;
  top: 20rpx; /* 假设第一个卡片大概位置 */
  left: 20rpx;
  width: 330rpx; /* 约半个屏幕宽度减间距 */
  height: 180rpx;
  border: 4rpx dashed #26A69A;
  border-radius: 24rpx;
  background: rgba(38, 166, 154, 0.1);
  box-sizing: border-box;
  animation: highlight-pulse 2s infinite;
}

.gesture-hand-wrapper {
  position: absolute;
  top: 110rpx; /* highlight中心附近 */
  left: 185rpx;
  z-index: 102;
}

.gesture-hand {
  font-size: 64rpx;
  line-height: 1;
  transform-origin: center center;
  animation: hand-press 1.5s infinite ease-in-out;
  text-shadow: 0 4rpx 8rpx rgba(0,0,0,0.2);
}

.gesture-hand::before {
  content: '👆';
  display: block;
}

.gesture-ring {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background: rgba(38, 166, 154, 0.4);
  transform: translate(-50%, -50%);
  z-index: 101;
  animation: ring-expand 1.5s infinite ease-out;
}

.gesture-bubble {
  position: absolute;
  top: 180rpx; /* 手指下方 */
  left: 100rpx;
  background: #374151;
  color: #ffffff;
  padding: 16rpx 24rpx;
  border-radius: 16rpx;
  font-size: 26rpx;
  font-weight: 500;
  box-shadow: 0 8rpx 20rpx rgba(0,0,0,0.15);
  z-index: 103;
  width: 400rpx;
  animation: bubble-float 3s infinite ease-in-out;
}

.gesture-bubble::after {
  content: '';
  position: absolute;
  top: -12rpx;
  left: 60rpx;
  border-width: 0 12rpx 12rpx 12rpx;
  border-style: solid;
  border-color: transparent transparent #374151 transparent;
}

@keyframes hand-press {
  0% { transform: scale(1) rotate(0deg); }
  50% { transform: scale(0.9) rotate(-10deg); }
  100% { transform: scale(1) rotate(0deg); }
}

@keyframes ring-expand {
  0% { transform: translate(-50%, -50%) scale(0.5); opacity: 1; }
  100% { transform: translate(-50%, -50%) scale(1.5); opacity: 0; }
}

@keyframes bubble-float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-6rpx); }
}

@keyframes highlight-pulse {
  0%, 100% { opacity: 0.8; border-color: #26A69A; }
  50% { opacity: 0.4; border-color: rgba(38, 166, 154, 0.5); }
}
