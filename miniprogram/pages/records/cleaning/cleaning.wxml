<!-- pages/records/cleaning/cleaning.wxml -->
<view class="cleaning-page">
  <!-- 通用头部组件 -->
  <app-header title="清洁记录" subtitle="鹦鹉清洁护理管理" showBack="{{true}}" theme="pink">
    <view slot="actions" class="header-btn" bindtap="addCleaningRecord">
      <text class="header-btn-icon">+</text>
    </view>
  </app-header>

  <!-- Content -->
  <scroll-view class="content" scroll-y="true">
    <!-- Summary Card (aligned with feeding stats design) -->
    <view class="summary-card">
      <text class="summary-title">清洁统计</text>
      <view class="summary-grid">
        <view class="summary-metric">
          <text class="summary-value">{{stats.weeklyCount}}</text>
          <text class="summary-label">本周清洁</text>
        </view>
        <view class="summary-metric">
          <text class="summary-value">{{stats.uniqueTypes}}</text>
          <text class="summary-label">清洁类型</text>
        </view>
        <view class="summary-metric">
          <text class="summary-value">{{stats.lastTimeText || '—'}}</text>
          <text class="summary-label">最近一次</text>
        </view>
      </view>
    </view>

    <!-- Filter Chips (aligned with health page design) -->
    <view class="filter-section">
      <scroll-view scroll-x="true" class="chip-scroll">
        <block wx:for="{{filterOptions}}" wx:key="*this">
          <view class="chip {{selectedFilter === item ? 'active' : ''}}" bindtap="selectFilter" data-filter="{{item}}">
            <text class="chip-text">{{item}}</text>
          </view>
        </block>
      </scroll-view>
    </view>

    <!-- Cleaning Records -->
    <view class="list" wx:if="{{displayRecords.length > 0}}">
      <block wx:for="{{displayRecords}}" wx:key="id">
        <view class="card">
          <view class="card-left">
            <image class="avatar" mode="cover" src="/images/default-parrot.png" />
          </view>
          <view class="card-right">
            <view class="card-head">
              <view class="info">
                <text class="name">{{item.parrot_name}}</text>
                <text class="type-badge">{{item.cleaning_type_text || item.cleaning_type}}</text>
              </view>
              <view class="time">{{item.cleaning_time_formatted}}</view>
            </view>

            <view class="card-body">
              <view class="detail" wx:if="{{item.description}}">
                <text class="label">描述：</text>
                <text class="value">{{item.description}}</text>
              </view>
              <view class="detail" wx:if="{{item.notes}}">
                <text class="label">备注：</text>
                <text class="value">{{item.notes}}</text>
              </view>
              <view class="detail" wx:if="{{isTeamMode && item.created_by_username}}">
                <text class="label">记录者：</text>
                <text class="value">{{item.created_by_username}}</text>
              </view>
            </view>
          </view>
        </view>
      </block>
    </view>

    <!-- Empty State -->
    <view class="empty" wx:if="{{displayRecords.length === 0 && !loading}}">
      <view class="empty-icon">🧹</view>
      <text class="empty-text" wx:if="{{isLogin}}">暂无{{selectedFilter}}记录</text>
      <text class="empty-text" wx:else>请先登录查看记录</text>
      <button class="btn btn-primary" wx:if="{{isLogin && hasOperationPermission}}" bindtap="addCleaningRecord">添加清洁记录</button>
    </view>

    <view class="loading" wx:if="{{loading}}">加载中...</view>
  </scroll-view>
</view>
